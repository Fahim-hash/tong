import Link from 'next/link';
import {articles} from './lib/data';
export default function Home(){const [hero,...rest]=articles;return <div className="wrap"><div className="ticker"><b>সর্বশেষ</b> টং-এর খবর এখন আরও দ্রুত, পরিষ্কার ও মানুষের কাছাকাছি</div><section className="leadgrid"><Link href={`/articles/${hero.slug}`} className="hero"><span className="eyebrow">{hero.category} • প্রধান খবর</span><h1>{hero.title}</h1><p>{hero.subtitle}</p><span className="meta" style={{marginTop:20,color:'#ccc'}}>{hero.author} • {hero.date}</span></Link><div className="side">{rest.slice(0,3).map(a=><Link className="story" href={`/articles/${a.slug}`} key={a.id}><span className="tag">{a.category}</span><h2>{a.title}</h2><p>{a.subtitle}</p><span className="meta">{a.author} • {a.date}</span></Link>)}</div></section><h2 className="sectiontitle">আজকের খবর</h2><div className="grid">{rest.map(a=><Link href={`/articles/${a.slug}`} className="card" key={a.id}><span className="tag">{a.category}</span><h3>{a.title}</h3><p>{a.subtitle}</p><span className="meta">{a.date}</span></Link>)}</div></div>}
