import {NextResponse} from 'next/server';
export async function POST(req:Request){try{const body=await req.json();const headline=String(body.headline||'').trim();if(!headline)return NextResponse.json({error:'headline is required'},{status:400});if(!process.env.GEMINI_API_KEY)return NextResponse.json({caption:`${headline} — বিস্তারিত জানতে টং-এর খবরের সঙ্গে থাকুন। #টংএরখবর`});const r=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key='+process.env.GEMINI_API_KEY,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({contents:[{parts:[{text:`Write one concise Bengali social news caption for this headline. No invented facts. Headline: ${headline}`}]}]})});if(!r.ok)throw new Error('AI provider error');const data=await r.json();const caption=data?.candidates?.[0]?.content?.parts?.[0]?.text||headline;return NextResponse.json({caption})}catch(e){return NextResponse.json({error:'Caption generation failed'},{status:500})}}
