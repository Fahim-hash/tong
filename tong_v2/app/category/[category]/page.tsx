import Link from 'next/link';import {articles} from '../../lib/data';
export default async function Category({params}:{params:Promise<{category:string}>}){const {category}=await params;const list=articles.filter(a=>a.category===decodeURIComponent(category));return <div className="wrap"><h1 className="sectiontitle">{decodeURIComponent(category)}</h1><div className="grid">{(list.length?list:articles).map(a=><Link className="card" href={`/articles/${a.slug}`} key={a.id}><span className="tag">{a.category}</span><h3>{a.title}</h3><p>{a.subtitle}</p><span className="meta">{a.author} • {a.date}</span></Link>)}</div></div>}
