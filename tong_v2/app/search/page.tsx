'use client';import {useMemo,useState} from 'react';import Link from 'next/link';import {articles} from '../lib/data';
export default function Search(){const [q,setQ]=useState('');const results=useMemo(()=>articles.filter(a=>(a.title+a.subtitle+a.category).toLowerCase().includes(q.toLowerCase())),[q]);return <div className="wrap"><h1 className="sectiontitle">খবর খুঁজুন</h1><div className="panel" style={{marginTop:22}}><input autoFocus placeholder="শিরোনাম, বিভাগ বা বিষয় লিখুন…" value={q} onChange={e=>setQ(e.target.value)}/></div><div className="grid">{results.map(a=><Link className="card" href={`/articles/${a.slug}`} key={a.id}><span className="tag">{a.category}</span><h3>{a.title}</h3><p>{a.subtitle}</p></Link>)}</div></div>}
